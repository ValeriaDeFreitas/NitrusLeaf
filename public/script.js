/*document.getElementById("btn-scan").addEventListener("click", () => {
    const resultMessage = document.getElementById("resultMessage");
    const messageElement = document.getElementById("message");

    // Exibe a mensagem "Escaneando..." até a resposta
    messageElement.textContent = "Escaneando... aguarde.";
    resultMessage.removeAttribute("hidden");
    messageElement.removeAttribute("hidden");


    // Envia a requisição POST para a rota /scanner
    // fetch("/home", {
    //     method: "POST", // Requisição POST
    //     headers: {
    //         "Content-Type": "application/json",  // Tipo de conteúdo é JSON
    //     },
    //     body: JSON.stringify({}) // Corpo vazio, já que é uma simulação
    // })
    //     .then((response) => response.json())  // Converte a resposta em JSON
    //     .then((data) => {
    //         // Exibe a mensagem de sucesso
    //         messageElement.textContent = data.message;  // "Documento escaneado com sucesso!"
    //     })
    //     .catch((error) => {
    //         // Caso haja erro na requisição, exibe uma mensagem de erro
    //         messageElement.textContent = "Erro ao tentar escanear o documento. Tente novamente.";
    //     });
});*/
